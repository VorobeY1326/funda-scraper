# Warning

This fork is customized for private use. Be careful if forking it:
- Geoapify dependency requires API key and may have licensing restrictions
- Transitous dependency can be used only in open source projects + requires a distinct user agent with your contact
